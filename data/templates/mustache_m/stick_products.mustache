{{#rows}}
    {{#product.is_product}}
    <div class="recom-cell three" id="product-{{ product._id }}">
        <a title="{{ product.title }}" alt="{{ product.title }}" href="{{ product.wap_view_url }}">
            <img class="lazy" data-original="{{ product.cover.thumbnails.apc.view_url }}" src="https://s3.taihuoniao.com/images/icon/load380-380nnm.png">
            <div class="desc">
                <h2>{{ &product.title }}</h2>
                <div class="cats">
                    <span class="unit">￥</span>{{ product.sale_price }}
                </div>
            </div>
        </a>
    </div>
    {{/product.is_product}}
{{/rows}}
