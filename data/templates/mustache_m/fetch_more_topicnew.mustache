{{#results.rows}}
	<div class="topicbox">
		<a href="{{ wap_view_url }}" title="{{ title }}" class="ui cover line">
        {{^has_asset}}
            {{#cover}}
            <img class="ui image" src="{{ cover.thumbnails.acs.view_url }}">
            {{/cover}}
        {{/has_asset}}
        {{#has_asset}}
            <img class="ui image" src="{{ cover.thumbnails.acs.view_url }}" >
        {{/has_asset}}
        </a>
        <div class="content">
            <h2 class="header {{#red}} topic-title-color-red{{/red}}{{#blue}} topic-title-color-blue{{/blue}}{{#green}} topic-title-color-green{{/green}}{{#yellow}} topic-title-color-yellow{{/yellow}}">
                <a href="{{ wap_view_url }}" title="{{ title }}" class="ui line">
    			    {{ attr_str }}{{ &title }}
    		    </a>
            </h2>

			<p class="author">
                <span class="mesg">
                    <a href="javascript:void(0);" class="ui link">@{{ user.nickname }}</a>
                </span>
                <i class="unhide icon"></i> <span>{{ view_count }}</span> <i class="comments outline icon"></i> <span>{{ comment_count }}</span>
			</p>
        </div>
    </div>
{{/results.rows}}
